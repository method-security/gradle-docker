name: Publish

on:
  push:
    branches:
      - develop
    tags:
      - '*'

jobs:
  checkstyle:
    name: Check
    uses: ./.github/workflows/check.yml

  build:
    name: Build
    uses: ./.github/workflows/build.yml

  # TODO: Uncomment and configure when ready to publish
  # publish:
  #   name: Publish Artifacts
  #   runs-on: ubuntu-latest
  #   needs: [check, build]
  #   if: github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/')
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #
  #     - name: Set up JDK 21
  #       uses: actions/setup-java@v4
  #       with:
  #         distribution: 'corretto'
  #         java-version: '21'
  #
  #     - name: Setup Gradle
  #       uses: gradle/actions/setup-gradle@v3
  #
  #     - name: Publish artifacts
  #       run: |
  #         ./gradlew --parallel --stacktrace --continue publish \
  #           -Porg.gradle.java.installations.fromEnv=JAVA_21_HOME,JAVA_HOME
  #       env:
  #         # TODO: Add required secrets for publishing
  #         # GRADLE_KEY: ${{ secrets.GRADLE_KEY }}
  #         # GRADLE_SECRET: ${{ secrets.GRADLE_SECRET }}

  check:
    name: ✅ Check
    if: always()

    needs:
      - checkstyle
      - build

    runs-on: ubuntu-latest

    steps:
      - name: Require all jobs to succeed
        uses: re-actors/alls-green@release/v1
        with:
          jobs: ${{ toJSON(needs) }}
